{
  "name": "porthub-domain-router",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that routes PortHub custom-domain traffic to the Railway app.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
